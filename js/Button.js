"use strict";

function Button(x,y,width,height,text){
    this.rect = {
        x:x,
        y:y,
        width:width,
        height:height
    };
    
    this.text = text;
}

function.prototype = {contructor: Button};

function.prototype.update = function(mouseX,mouseY){
    
};

function.prototype.draw = function(ctx){
    
};